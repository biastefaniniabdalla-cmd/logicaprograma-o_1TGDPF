using UnityEngine;

public class Ex25 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int bronze = 12 * 1;
        int prata = 8 * 5;
        int ouro = 3 * 10;
        print ("total : "  + (bronze  + prata + ouro));
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

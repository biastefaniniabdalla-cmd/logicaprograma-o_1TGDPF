using UnityEngine;

public class Ex4 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int moedas = 50;

        float quantPjog = moedas / 5 ;
        print("quantidade por jogador é de  " + quantPjog);
        
    }


}

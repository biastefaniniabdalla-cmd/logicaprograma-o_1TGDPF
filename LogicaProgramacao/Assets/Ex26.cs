using UnityEngine;

public class Ex26 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        float distancia = 120;
        float tempo = 10;
        float velocidadeMed = distancia / tempo;
        print("velocidade: " + velocidadeMed );
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
